# Normalise line endings in the repo so a Windows checkout does not produce a
# whole-file diff for everyone else. Text is stored LF, checked out native.
* text=auto eol=lf

# Binary — never touch these.
*.png binary
*.jpg binary
*.ico binary
*.woff binary
*.woff2 binary
*.ttf binary
*.otf binary
